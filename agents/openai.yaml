interface:
  display_name: "自适应多模型编排器"
  short_description: "为自主项目评估协作收益，按任务分工与传递背景，重视整体验收"
  default_prompt: "Use $adaptive-model-orchestrator to design the smallest effective model team and coordinate this task through integrated validation."

policy:
  allow_implicit_invocation: true
